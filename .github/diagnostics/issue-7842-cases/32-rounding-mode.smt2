(set-logic ALL)
(declare-datatype Box ((B (value RoundingMode))))
(declare-const x Box)
(assert (distinct x (B RTZ)))
(assert (distinct (value x) RNE))
(assert (distinct (value x) RNA))
(assert (distinct (value x) RTP))
(assert (distinct (value x) RTN))
(check-sat)
